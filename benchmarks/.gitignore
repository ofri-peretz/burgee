# Generated per run — the fixtures are a projection of fixtures/entry-points.ts
.fixtures/

# Raw agent transcripts. One exemplar per task is committed by hand for the article;
# the rest are large, per-run, and say nothing a reader needs.
results/transcripts/

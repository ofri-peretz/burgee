// The floor (intent constraint 5). No parser at all — Node starting up and writing a
// line. Most of every other row is this, and a table without it credits the frameworks
// with Node's own startup cost.
process.stdout.write('Hello, ada!\n');

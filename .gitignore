node_modules/
dist/
.turbo/
coverage/
.next/
.source/
next-env.d.ts
*.tsbuildinfo
.env
.env.local
.vercel/
# dev-loop test fixtures, written per run
.dev-fixtures/

# eval transcripts and whatever a case asked the agent to write
evals/results/

# The competitor watch writes its report here on every run (upstream-watch R4);
# it is a build artifact, like packages/compat-oracle/upstream.json.
packages/compat-oracle/competitors-upstream.json

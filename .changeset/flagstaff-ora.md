---
"flagstaff": patch
---

`flagstaff/ora` — ora 9's whole API, graded 99 / 99 by ora's own test suite. One import changes; the seventeen packages ora ships become two, 112.7 KB of JavaScript becomes 63.6 KB, and the spinner corpus, the display width, the log symbols, the cursor control and the stdin discarder come with it.

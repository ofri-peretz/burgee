/**
 * closeout — name reserved; planned as wave F2 of the foundation tier. The intent and
 * design are at .sdlc/intents/closeout/ in the burgee repository, both at draft. Until
 * they are approved and built, this entry exports only its own name, so that
 * importing it costs nothing and promises nothing.
 */
export const name = 'closeout' as const;

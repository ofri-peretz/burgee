/**
 * caique — reserved. The first working release follows
 * docs/intents/caique/ in the burgee repository. Until then this entry
 * exports only its own name, so that importing it costs nothing and promises nothing.
 */
export const name = 'caique' as const;

/**
 * linegauge — name reserved; planned as wave F1 of the foundation tier. The intent and
 * design are at .sdlc/intents/linegauge/ in the burgee repository, both at draft. Until
 * they are approved and built, this entry exports only its own name, so that
 * importing it costs nothing and promises nothing.
 */
export const name = 'linegauge' as const;

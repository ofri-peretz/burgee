{
  "name": "demo-cli-large",
  "version": "0.0.0",
  "private": true,
  "description": "A 30-command CLI across 5 groups with lazily loaded handlers: the fixture for cli-modularity's lock — help and --schema import zero handler modules, one command imports exactly one.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "bin": {
    "demo-large": "./dist/bin.js"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src"
  },
  "dependencies": {
    "burgee": "*"
  }
}

{
  "$comment": "Release budgets (cli-benchmarks / Y8). A ratchet that moves per PR is a record of what happened, not a limit on it \u2014 `benchmarks/axes/weight.ts` says so above RATIO_CEILING after four raises in one session. A budget lives here instead: set once for a release, from the measured distribution rather than from the last commit, and `scripts/release-budget-lock.test.ts` refuses to let a number here move unless `release` moves with it in the same commit.",
  "release": "wave-5",
  "setOn": "2026-09-23",
  "budgets": {
    "bundled-bytes-ratio:burgee\u00f7cac": {
      "value": 2.9,
      "unit": "ratio",
      "why": "burgee's core bundle over cac's, the lightest zero-dependency incumbent in the field. cac has no --schema, no MCP surface and no manifest, so the denominator is a smaller package rather than a lighter one doing the same work; the ratio is published because a suite that only showed the five entry points we win is lying by selection. Raised 3.7 -> 3.9 on 2026-09-15 for PLAN 3.2's cosmiconfig surface. Lowered 3.9 -> 2.8 for wave-4 on 2026-09-21: measured 2.740 after the root barrel stopped re-exporting five optional modules (D-101), under the metric that counts the whole startup graph rather than the entry chunk (D-100). A ceiling left at 3.9 would be headroom nobody decided to grant. D-102 records that <= 1 is not reachable by engineering: cac is 10,457 bundled bytes of parser and help renderer, and the measured floor for an engine that also coerces values, resolves configuration, binds shutdown and detects an agent is near 2.26x. Raised 2.8 -> 2.9 for wave-5 on 2026-09-23 (D-139): the engine wave, spent by name below.",
      "derivedFrom": {
        "band": "core-bundled-bytes",
        "observations": 41,
        "mean": 35740.4,
        "sd": 1082.1,
        "latest": 37196,
        "rule": "mean + 3 sd = 38,987 bytes, which is 3.73x cac's 10,452. Rounded down to 3.7: three sigma is where the band itself calls a single point an act-now breach, so a budget above it would admit growth the watcher already refuses.",
        "note": "This band breached at 1 sigma on 2026-09-10 \u2014 Rule 4, eight consecutive above the mean. The budget is set from the distribution anyway, and deliberately: a budget derived from a drifting series still bounds the drift, and lowering it is the next release's decision to take with the trend in hand."
      },
      "deviation": {
        "from": 2.8,
        "why": "Spent, not derived: `core-bundled-bytes` still holds no observation under the D-100 metric (its history ends 2026-09-15), so there is no spread to take three sigma of, exactly as when 2.8 was set. What the room buys, each measured on main's 29,116 B: S4 `-` as stdin +93 (D-113), D3 dynamic completion +135 (D-119), V8 `config explain` +303 (D-117), F1 program-schema.json +68 (D-123), P2/P3 named exit codes +266 (D-120), and D-140's single --json failure envelope, about +130. Together about 30,111 B, 2.881 of cac's 10,451. 2.9 holds 30,308 B: the wave fits with under 200 B to spare, and the next engine feature does not \u2014 it pays for itself in bytes, or it names wave-6. The next release re-derives this from the band once it holds observations under the new metric."
      },
      "note": "Was BREACHED at 5.348 through the composition wave; it is not now. Measured 2.740 on 2026-09-21 with the core at 28,637 bundled bytes against cac's 10,457, which is the lowest this ratio has ever read. One sentence of the old note is retracted: it said lazy-loading help \"measures +767 bytes rather than fewer, because esbuild inlines a literal-specifier dynamic import and then charges for the wrapper\". That was true of the instrument, not of the code \u2014 `--outfile` has no chunks, so it inlined every dynamic import and charged the wrapper on top. Under `--splitting --outdir` the same laziness is worth what it always was, and the barrel split it enabled took the core from 57,880 to 28,637. The general lesson is the one this repository keeps relearning: a measurement that cannot see the thing you are optimising will tell you the optimisation does not work."
    }
  }
}

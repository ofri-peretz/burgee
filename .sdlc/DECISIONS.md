# Decisions

**Every decision in this repository is closed here, with the answer and the date.** A
decision that lives only in a chat log cannot be reviewed, diffed, or replayed by the next
agent — that is rule 1 of `CLAUDE.md` one level above this repo, and a question carried at the end of a
message violates it exactly as much as a design does.

This file exists because 102 open questions had accumulated across 48 intents with no
mechanism that could ever close one. An intent's `## Open questions` section is where a
question is *raised*. This file is where it *ends*.

`scripts/decisions-lock.test.ts` is what makes that a mechanism rather than a sentence. It
checks four things and no more: the repo-wide open-question count stays at or under the
ceiling in `.sdlc/bands/open-questions.json`, no row here is half-written, no id is used
twice, and every `Superseded by` names a decision that exists. It does **not** verify that a question closed here was struck from the intent that
raised it — that is step 3 below and it is on the author, because matching a table row to a
prose bullet is a guess and a gate that guesses is worse than none.

## Who decides

**The default is to decide.** An agent working in this repository takes the decision, writes
it down here, and continues. It does not end a message with a question it could have
answered. Four kinds go to the owner and nothing else does:

| Escalate | Because |
| :--- | :--- |
| A **published claim** changing — a number or promise on a README, the docs site, or npm | It is a promise to people outside the repo, and retracting one costs more than making it |
| A **band or ceiling** moving in the loosening direction | Ratchets exist so growth is a decision; an agent raising its own ceiling is the failure the ratchet was built to catch |
| A **package's identity** — what it is for, what it refuses to be | caique not wrapping clack is the reason caique exists; that is not an implementation detail |
| **Money, publishing, or anything outward-facing** — npm, a metered API, an upstream PR | Irreversible, and not the agent's to spend or say |

Everything else is taken. A tie is broken by: the option the tree can already prove, then
the smaller diff, then the one that is easier to reverse. If a decision later turns out
wrong, it is reversed here with the reason — which is cheaper than every session re-deriving
the same answer and cheaper still than never deciding.

**Taken** below means the agent decided and it stands. **Accepted** means the owner did.
An escalated decision sits in the table with `Owner` in the fourth column and a default
beside it, and the default is what happens until the owner says otherwise — a question with
no answer is a decision to do nothing, made slowly.

| D-001 | clack's compat row: publish a stated subset or the raw measurement | **Stated subset**, the `cli-table3` shape — subtract with the reason in `conditionalCases`. 289 of clack's 444 assertions snapshot its exact drawing; a façade matching them would *be* clack, which caique's design rejects (U3). The behavioural remainder is `limit-options` (14) + `guide` (3) | Taken | 2026-09-20 | — |
| D-002 | One intent for the façade programme, or one per façade | **One** — `facade-per-incumbent`, with per-package requirements appended to existing designs. The finding is uniform; eleven near-identical intents is ceremony, not a handoff | Taken | 2026-09-20 | — |
| D-003 | caique, paratext and closeout have no status ledger: backfill fully or only the façade rows | **Fully.** A ledger covering three rows of twenty-three reads as complete and tells the reader less than no ledger at all | Taken | 2026-09-20 | — |
| D-004 | `burgee/meow`, `burgee/cac`, `burgee/citty` — in the programme or out | **In as requirements, last in the order.** Writing the requirement costs nothing and stops them being invisible; building them waits behind rows with real gaps | Taken | 2026-09-20 | — |
| D-005 | The `core-under-52kb-bundled` claim, broken at 57,880 against 53,248 | **Restate at the measurement**, naming `completions.js` (7,763 B, inlined by esbuild under `--outfile`) as the largest single item. Making `--mcp`, `--schema`, `completion` and plugins opt-in to hit a byte count would remove the reason burgee exists | Taken — reversible by the owner, it is a published claim | 2026-09-20 | — |
| D-006 | Façade subpath or package root as the compat target | **Subpath, always.** Every row at 100% targets a dedicated subpath; every row at or near zero targets the root, which presents the package's own native API and can never match an incumbent's | Taken | 2026-09-20 | — |
| D-007 | When a façade may be named in a host row | **Only once it exists.** Naming an unbuilt façade publishes "target not built yet" where a measured number belongs — the lesson `cli-table3`'s note already records | Taken | 2026-09-20 | — |
| D-008 | What burgee leads with | **What it replaces, never speed.** `npm i burgee` stands in for twelve packages and their trees against one with no runtime dependencies. burgee starts at 2.57x cac and bundles 5.5x it; leading with speed invites the benchmark it loses. See `.sdlc/research/migration-drivers.md` §3, §4 | Taken | 2026-09-20 | — |
| D-009 | Whether to ship a codemod | **Yes — `burgee migrate`.** Every migration that actually happened shipped one before the wave, not after: `jest-codemods`, `pnpm import`, `biome migrate eslint`. "Change one import" is already the strongest version of this claim in the family; the codemod is what turns it from *could* into *did, in four minutes* | Taken | 2026-09-20 | — |
| D-010 | `--mcp`'s place in the positioning | **Table stakes, not the moat, and never what byte budgets are cut to fit.** A CLI costs an agent nothing until called; an MCP server's tool definitions are loaded before any work happens and cost tokens every turn whether used or not. The moat is a CLI legible without a server — `--schema`, `--json`, exit codes, and `fix:` naming a command rather than describing a problem. See §6 | Taken | 2026-09-20 | — |
| D-011 | `--schema` output shape | **Progressive disclosure: one command's contract, not the whole tree.** A `--schema` that dumps every command is the MCP mistake in CLI clothing — paying up front for what is not used. `commandSchemaOf` already scopes to one node; the flag must too | Taken | 2026-09-20 | — |
| D-012 | The highest-value unbuilt thing | **Agent tokens per completed task, burgee CLI against the same capability as an MCP server.** The `agent-tokens-per-task` and `agent-turns-per-task` bands exist and have never run. A first-party number on a live industry argument, made credible by the same discipline as the compat rows | Taken — needs a credential, which is the owner's | 2026-09-20 | — |
| D-013 | bellpull: streaming | **Out of scope until `execa` is a graded host; a subpath (`bellpull/stream`) when it is.** A result-shaped API and a streaming API are different products, and nothing grades streams today | Taken | 2026-09-20 | — |
| D-014 | bellpull: how much of execa's surface | **The `run` result shape only.** The template-literal `` $`cmd` `` form is sugar over the same result and a different product shape; grading the result covers the behaviour | Taken | 2026-09-20 | — |
| D-015 | bellpull: does `open` fold in | **No.** It launches a URL or file in the desktop rather than running a child for its output, and it would add a platform-detection surface this layer otherwise does not have. One package, one problem | Taken | 2026-09-20 | — |
| D-016 | bellpull: is `duration` honest | **Drop it.** Wall-clock from spawn to close includes Node's own scheduling, so publishing it as the child's cost is a number we cannot defend. Measured or it is a slogan | Taken | 2026-09-20 | — |
| D-017 | closeout: the default deadline | **2000 ms, finite, and revisited against measurement.** Long enough for a filesystem flush and an in-flight request to abort, short enough that an agent is never stuck. Finite by design, so the failure mode is a truncated flush rather than a hang | Taken | 2026-09-20 | — |
| D-018 | closeout: is `exitCode` preserved on every path | **Yes, unconditionally — a handler must never turn 3 into 0.** If `signal-exit`'s suite does not assert it, it becomes one of our own conformance cases | Taken | 2026-09-20 | — |
| D-019 | closeout: does `once` / `onetime` belong here | **Keep it.** Exactly-once is the package's whole thesis and its name; 162 M/wk of demand for it is the argument. Widening slightly past the name is cheaper than a package of one function | Taken | 2026-09-20 | — |
| D-020 | closeout: worker threads and `beforeExit` | **Per-thread, and documented as per-thread.** Being narrow is better than being wrong, and a cross-thread exactly-once claim we cannot test is the wrong kind of promise | Taken | 2026-09-20 | — |
| D-021 | seniority: does `resolve` stay synchronous | **Yes.** `fs.existsSync` in a bounded loop keeps the API trivial and the sync half is what cosmiconfig's suite grades. A dual API is added only when a graded host actually needs one, never speculatively | Taken | 2026-09-20 | — |
| D-022 | seniority: how much of cosmiconfig's search semantics | **Whatever its suite grades, and every divergence listed in `conditionalCases` with a reason.** That is already the doctrine for every other host; nothing here is special | Taken | 2026-09-20 | — |
| D-023 | seniority: does `env-paths` belong in seniority or closeout | **Neither.** OS config-directory resolution is neither precedence nor lifecycle, and folding it in widens both layers past their names | Taken | 2026-09-20 | — |
| D-024 | linegauge: does `wrap` reopen styles per line | **Match `wrap-ansi`, because its suite is the grader.** Where the suite does not cover the trailing-whitespace disagreement, the divergence is written down rather than resolved by preference | Taken | 2026-09-20 | — |
| D-025 | linegauge: is `ansi-regex` a subpath or an internal | **Internal.** Exporting a regex is a compatibility liability forever — every future fix to it becomes a breaking change. 345 M/wk is a reason to be careful, not a reason to publish | Taken | 2026-09-20 | — |
| D-026 | roundel: does `./policy` become its own package | **Stays in roundel.** `flagstaff` peering on `roundel` for one function is acceptable; a package of one function is not an independent product, which is the bar rule 8 sets | Taken | 2026-09-20 | — |
| D-027 | roundel: `chalk.level` and `chalkStderr` against one policy | **Both honoured, both as views of the one policy** — `level` reads and writes the policy rather than shadowing it, and `chalkStderr` is a second instance bound to stderr. Two models would be two answers to what colour is on | Taken | 2026-09-20 | — |
| D-028 | flagstaff: plugin discovery | **Explicit `register()` only.** Z1 says nothing may require a config file, and a `burgee.plugins` array in `package.json` is a config file wearing a different hat. Discovery is added only if `cli-modularity` needs it for its own plugins | Taken | 2026-09-20 | — |
| D-029 | flagstaff: character and mascot plugins | **A spinner with named states — no new key.** A state-keyed frame set is the same shape as a spinner with more states, and the smaller answer is the right one | Taken | 2026-09-20 | — |
| D-030 | paratext: does `image` accept a path | **Buffer only.** The caller reads the file and owns the I/O, which keeps `node:fs` out of a package that otherwise touches nothing | Taken | 2026-09-20 | — |
| D-031 | paratext: where the terminal Support table lives | **A data file a plugin can replace wholesale.** It is the part that rots, and rot belongs where it can be replaced without a release | Taken | 2026-09-20 | — |
| D-032 | seniority: what `config-layers` actually does | **F3 does not open until it has been read.** Published the day seniority was measured, so it is a prior-art check rather than a design choice; it becomes a requirement in seniority's design, not a standing question | Taken | 2026-09-20 | — |
| D-033 | linegauge: the honest fast-path threshold | **Whatever the measurement says.** `Intl.Segmenter` at 3.5 µs per 50-character segmentation is one point, not a curve; the ASCII fast path is gated at the crossover the benchmark finds, and the benchmark is a requirement | Taken | 2026-09-20 | — |
| D-034 | compat-grader-layer: the package name | **Owner.** Default until they say otherwise: **`loadline`** — free on npm as of 2026-09-14, nautical like the rest of the family, and the metaphor is exact: a published limit, verified by survey, that you may not cross | Owner — default stands | 2026-09-20 | — |
| D-035 | compat-grader-layer: does the grader leave this repo | **Owner**, because it is a rule-10 identity call. Default: **it stays, burgee-family only.** Making it its own family means the 322 vendored suites travel with it, which is a far larger move than a rename, and until someone pays for that move `eslint/` keeps making compatibility claims with no instrument | Owner — default stands | 2026-09-20 | — |
| D-036 | compat-grader-layer: what happens to `conformance` | **No change — private, adjacent, out of this intent's scope.** It was never covered by this intent's evidence and inventing coverage for it here would be a claim with nothing behind it | Taken | 2026-09-20 | — |
| D-037 | compat-grader-layer: do the vendored suites ship in the tarball | **No.** 322 files of other people's tests under their own licences is a licence question, and shipping them by default answers it in the riskiest direction. The grader ships with `vendor-suite`, which already fetches them on demand | Taken | 2026-09-20 | — |
| D-038 | compat-grader-layer: the four changesets | **Retired.** Their prose already lives in the commits that introduced them, and a changeset that restates a landed commit inflates a release note without informing anyone | Taken | 2026-09-20 | — |
| D-039 | upstream-watch: where the fingerprint lives | **`packages/<pkg>/competitors.json`, one file per package**, holding the declaration and the last-seen fingerprint — so a competitor moving is a git diff a human reads in a PR. A central file is one thing to lock and breaks constraint 5 | Taken | 2026-09-20 | — |
| D-040 | upstream-watch: does the surface diff read `.d.ts` or exports | **Both, `.d.ts` preferred when present.** A type-only addition is a real API addition and is invisible to a runtime export scan | Taken | 2026-09-20 | — |
| D-041 | upstream-watch: does a stale weight claim fail CI | **Open an issue, never fail the build.** A competitor getting heavier is not our regression, and a red build we cannot fix by changing our own code is a broken feedback loop | Taken | 2026-09-20 | — |
| D-042 | upstream-watch: do `hosts.ts` and `competitors.json` merge | **No.** A host is something we run a *test suite* from and needs a repo, a runner and a shim; a competitor is something we measure a *surface* against and needs only a name. Every host is a competitor; most competitors are not hosts | Taken | 2026-09-20 | — |
| D-043 | brand-burgee: does it belong in this repo | **Owner.** Default: **it moves to `interlace`.** It shares the "one declaration, many projections" thesis and nothing else — not argv, not the manifest — and rule 10 is one repo per family | Owner — default stands | 2026-09-20 | — |
| D-044 | brand-burgee: is a supplied glyph embedded in the field | **No — the generator emits the two-tone field only.** Keeping a raster legible at 16px inside an SVG is a different problem from colouring a locked shape, and solving it by accident is how a generator stops being deterministic | Taken | 2026-09-20 | — |
| D-045 | brand-burgee: is the generator itself a burgee CLI | **Yes.** It is the honest dogfood — a real command answering `--json`, `--schema` and `--mcp` like any other — and a generator that is not one would be an argument against our own thesis | Taken | 2026-09-20 | — |
| D-046 | brand-burgee: which raster formats, against which rasteriser | **PNG only, at 1x and 2x.** The rasteriser is whichever the docs site already depends on; adding a new one is a dependency decision under rule 2 and does not ride along with a format list | Taken | 2026-09-20 | — |
| D-047 | cli-foundation-stack: does `seniority` move ahead of `linegauge` | **Yes, pull it forward.** Biggest layer at 1.81 B/wk and the cheapest to start because `precedence.ts` already exists; the criterion for pulling it forward is already recorded in `spec.md` | Taken | 2026-09-20 | — |
| D-048 | cli-foundation-stack: does `bellpull` survive a re-check against `tinyexec` | **Yes, and the re-check is in.** bellpull grades 68 / 68 on cross-spawn's own suite *and* fixes the Windows `PATHEXT` defect upstream still carries. The weight pitch was never the pitch (D-008), so `tinyexec` owning it is not a kill signal | Taken | 2026-09-20 | — |
| D-049 | cli-foundation-stack: does `config-layers` become a competitor | **Yes — it goes in `packages/seniority/competitors.json`** under D-039. Somebody publishing into your layer the week you measure it is exactly what the watch exists to notice | Taken | 2026-09-20 | — |
| D-050 | `burgee migrate`: parser or specifier scan | **Specifier scan, no parser.** Rule 2 forbids a runtime dependency and the rewrite surface is a quoted string in five known positions. The cost is that unclassifiable positions are refused rather than handled, and that cost is paid in a report rather than silently | Taken | 2026-09-20 | — |
| D-051 | `burgee migrate`: what happens to a file it cannot fully migrate | **Nothing — the whole file is left untouched and listed.** A half-migrated file fails later as a runtime error in someone else's CLI, and burgee gets blamed first. The unit of success is the file | Taken | 2026-09-20 | — |
| D-052 | `burgee migrate`: interactive or not | **Non-interactive by default.** The second audience is an agent migrating a repository unattended; a prompt is a wall. Safety comes from refusing a dirty git tree and from `--dry-run`, not from asking | Taken | 2026-09-20 | — |
| D-053 | `burgee migrate`: does it rewrite API calls too | **No — imports only.** `program.parse()` is the same call on both sides; that is what 1360 / 1360 means. Editing call sites would be claiming the façade is not drop-in, which is the opposite of the product | Taken | 2026-09-20 | — |
| D-054 | `burgee migrate`: does it edit `package.json` | **No.** The report says which dependencies became removable; removing them is the maintainer's commit. A codemod that edits a manifest is a codemod that resolves a lockfile, and that is not this | Taken | 2026-09-20 | — |
| D-055 | `burgee migrate`: how fast is fast enough | **A measured budget, not an adjective: 1,000 files under 500 ms, any single file under 1 ms**, gated on the number. One text pass, no AST — which costs two orders of magnitude less *and* costs no dependency, so the fast choice and the rule-2 choice are the same choice. A codemod a person watches is one they run once and never again | Taken | 2026-09-20 | — |
| D-056 | docs-per-package: what happens to `cli.interlace.tools` | **Owner** — it is DNS and a Vercel project, not code. Default: **301 to `burgee.interlace.tools` and keep it as a family alias.** A retired host breaks every link anyone has already shared, and an alias costs a DNS record | Owner — default stands | 2026-09-20 | — |
| D-057 | docs-per-package: does caique get its own host | **Owner**, because it is a grouping call about the family's shape. Default: **a section on `seniority.interlace.tools`, per the bar.** caique is 0 / 606 against clack and 0 / 41 against inquirer-core; nothing thin ships, and the bar already says so | Owner — default stands | 2026-09-20 | — |
| D-058 | docs-per-package: does the five-site map hold at fifteen packages | **No, and it is not meant to — the map is a judgement per package, not a formula.** A package whose incumbent nobody searches for is a section; one replacing something with chalk's traffic is a host. Criterion 1 enforces only that every published package is *somewhere* | Taken | 2026-09-20 | — |
| D-059 | docs-per-package: the affected gate marks all eleven workspaces changed | **Accept the redundant deploys; do not open an intent.** The cause is the root `devDependencies` on burgee, compat-oracle and flagstaff, which are there for their own reasons, and the cost is a handful of no-op deploys per merge. The opposite error — a change reaching *no* app — is the one worth engineering against, and the design already mitigates it | Taken | 2026-09-20 | — |
| D-060 | docs-per-package: four GitHub Environments or one | **Per-app.** A reviewer approving a deploy should be able to see which site they are approving; adding reviewers in four places is the cheaper mistake | Taken | 2026-09-20 | — |
| D-061 | agent-headroom: is compact the default for `--schema` | **Default.** Its reader is a machine by construction (N8), and `--format=json-pretty` covers the human debugging it | Taken | 2026-09-20 | — |
| D-062 | agent-headroom: where the field budget lives | **On any machine format, not only `--schema`.** A large result set has exactly the problem a large schema does, and a budget that knows about only one of them is a budget in the wrong place | Taken | 2026-09-20 | — |
| D-063 | agent-headroom: does `meta.provenance` stay in the default envelope | **Yes, and `--fields` drops it.** It is the whole point of `diagnose-provenance` and dead weight everywhere else: a default that is right for the task that needs it and projectable away by the tasks that do not | Taken | 2026-09-20 | — |
| D-064 | control bands: is the bundled-bytes breach a regression, a measurement change, or a corpus change | **A real regression, already attributed.** Both bands are weight drifting up, and the cause is the `core-under-52kb-bundled` overrun in D-005: 57,880 against 53,248, of which `completions.js` is 7,763 B inlined by esbuild under `--outfile`. Not a change in what we measure, and not the corpus | Taken | 2026-09-20 | — |
| D-065 | control bands: which commit first left the band, and where that answer lives | **The results files, read by a bisect over `benchmarks/results/` — not this intent.** A band intent records that a band breached and what it means; it is not where an investigation is carried out, or every band acquires a standing question nobody closes | Taken | 2026-09-20 | — |
| D-066 | plugin-contract: does `tokens` accept hex only or also styleText names | **Both — hex is contrast-checked, names are not** (roundel R5). Refusing names would make the common case the awkward one, and a name has no channel values to check | Taken | 2026-09-20 | — |
| D-067 | plugin-contract: do third-party plugins get an npm namespace | **No — a `keywords` convention only.** The gallery indexes by keyword, so a name prefix buys nothing and costs every author a rename | Taken | 2026-09-20 | — |
| D-068 | caller-matrix: the sixth caller | **Record both — a web terminal and a chat surface — and build neither until an adopter names one.** A caller nobody has asked for is a shape we would be guessing at, and the matrix exists to stop guessing | Taken | 2026-09-20 | — |
| D-069 | cli-foundation-stack: where the `Runtime` seam lives for four packages that may not import burgee | **Each declares its own structural type.** Four small definitions of one shape beats a fifth package existing only to hold a type — rule 8 says a package is an independent product, and a type-only package is not one | Taken | 2026-09-20 | — |
| D-070 | output-stack-compat: does picocolors deserve a row | **One row, graded once, never re-vendored unless it changes.** Nine functions is a small surface, and a row that exists is worth more than an argument about whether it should | Taken | 2026-09-20 | — |
| D-071 | output-stack-research: do GitHub *discussions* count as a tracker | **Yes — read them and cite them as discussions.** Several of these repos push feature talk there rather than into issues, so excluding them would miss the demand signal and call it absence of evidence | Taken | 2026-09-20 | — |
| D-072 | The twenty-eight items filed under "Open questions" that were never questions | **Closed as records.** Twelve intents kept decisions made at finalisation in the section for *raising* them — the names in `cli-output-stack`, the floor adoptions in six others, the harness shapes in `cli-testing-harness`. They are marked decided and kept as the record they always were, rather than deleted | Taken | 2026-09-20 | — |
| D-073 | The seven weight and cold-start ratchets the tree is over | **Owner — do not raise them.** Raising seven ceilings at once is precisely the failure `weight.ts` already names: "a ceiling moved per PR is a record of what happened, not a limit on it", written after four raises in one session. The tree is over its ratchets; that is a fact about the tree, not about the ratchets. Default while the owner decides: **leave them red and say so on every report** rather than move them quietly | Owner — default stands | 2026-09-20 | D-099 |
| D-074 | Whether B4 `bundled-bytes` measures what it claims | **Owner**, because it changes a published instrument. The axis says it measures "what a user's application grows by", and it bundles with esbuild `--outfile`, which **inlines** `await import()` — so `completions.js` (7,763 B) is counted as startup weight when every real bundler with code splitting emits it as a chunk that is not loaded at startup. The measurement is over-counting against its own stated purpose. Default: **leave the instrument alone.** Changing a measurement while it is failing is the one move this repository has the most scar tissue about, whatever the merits, and the merits here are real enough to deserve a decision rather than a patch | Owner — default stands | 2026-09-20 | D-099 |
| D-075 | Single ratchets a lane must move to land measured surface (paratext artifact size, flagstaff `./cli-table3` bytes) | **Taken, one at a time, each priced at the point of change.** This is not D-073. That refused **seven** ceilings raised at once with no attribution, to paper over drift the tree had already accumulated. These are single bumps, each buying a measured row: paratext +17.4% gzipped for `terminal-link` 0→8/10 and `term-img` 0→12/18, flagstaff `./cli-table3` 29,000→29,300 B for internals 0→90/104. Reverting either number means reverting the rows. The test is whether the bytes bought something the oracle can see, and here they did | Taken — owner-reversible | 2026-09-20 | — |
| D-076 | The playbook this repository's SDLC follows, pinned | **Anthropic's AI-Native SDLC playbook**, published 2026-08-21, at https://claude.com/blog/the-ai-native-sdlc-playbook. `AI_NATIVE_SDLC.md` one level above this repo is our conformance statement against it, not a copy of it — where the two disagree the playbook wins and the deviation gets a row here | Accepted | 2026-09-20 | — |
| D-077 | Stage 2's artifact name: keep `design.md` or take the playbook's | **`spec.md`.** The playbook names both files explicitly — "Commit `spec.md` alongside `intent.md`" for Design, and "Commit the approved plan as `plan.md`" for Build. `design.md` is ours, occupies no stage, and stands where a reader looking for the Design artifact would look; renaming it to `plan.md` would take the name Stage 3 still needs | Taken | 2026-09-20 | — |
| D-078 | How far the `design.md` rename reaches | **All three repositories that share the convention, authority last: `eslint/` (130 files), `burgee/` (44, done), `agents/` (4), then `AI_NATIVE_SDLC.md` (10 references).** `.sdlc/intents/README.md` asserts the layout is the same as `eslint/`, so renaming one repo alone breaks the invariant it states. The authority moves last so it never describes a state that does not exist | Taken | 2026-09-20 | D-092 |
| D-079 | What becomes of `.sdlc/PLAN.md` | **Stays.** It is a programme plan across ten lanes, not the playbook's per-change `plan.md`. The collision is hypothetical until per-intent plans exist | Taken | 2026-09-20 | — |
| D-080 | `.sdlc/intents/` or the playbook's `intent/` | **`.sdlc/intents/` stays.** The playbook says the *simplest* home is an `intent/` folder — a suggestion, not a name. `.sdlc/` groups all six stages' artifacts, and moving it is a second hundred-reference sweep for no conformance gain | Taken | 2026-09-20 | — |
| D-081 | This ledger's own standing under the playbook | **A declared extension.** The playbook's audit trail is intent, spec, plan, diff, findings — it has no decision record at all. This file exists because 102 open questions had no closing mechanism, which is a gap the playbook leaves rather than a rule it sets | Taken | 2026-09-20 | — |
| D-082 | The real Stage 3 gap | **A per-change `plan.md` naming the files, the order and the tests that prove it.** `AI_NATIVE_SDLC.md` already self-reports it as output not committed — plan mode runs and its output is discarded, so a diff has nothing to be reviewed against | Taken | 2026-09-20 | — |
| D-083 | How much of the superpowers plugin to adopt | **Stage 3 only** — `writing-plans`, `executing-plans`, `test-driven-development`, `verification-before-completion`, `requesting-code-review`. Its `writing-plans` output is the `plan.md` D-082 names. `brainstorming` is rejected: it writes a competing spec to `docs/superpowers/specs/<date>-<topic>-design.md`, duplicating 44 existing specs under the one name D-077 removes | Taken | 2026-09-20 | — |
| D-084 | superpowers install scope | **`local` to this repo.** v6.3.0 is already installed, scoped to `agents/footprint`. It mandates TDD and worktrees, which the other repositories never agreed to | Taken | 2026-09-20 | — |
| D-085 | Its hardcoded `docs/superpowers/plans/` path | **Default on the trial, then a path-scoped `.claude/rules/` file redirecting to `.sdlc/intents/<slug>/plan.md`.** Measure the artifact before relocating it, and redirect with a rule rather than forking a skill we do not maintain | Taken | 2026-09-20 | — |
| D-086 | What to trial superpowers on | **`closeout`** — compat row 0 of 3, three small incumbents, an approved spec, one lane, real tests. A first trial wants a floor to rise off, not a package whose numbers are already good | Taken | 2026-09-20 | — |
| D-087 | `using-git-worktrees` against `LANES.md` | **`LANES.md` wins.** Ours is enforced by `lane-boundaries-lock.test.ts`; theirs is a skill. A gate beats a suggestion, and two worktree disciplines is one too many | Taken | 2026-09-20 | — |
| D-088 | Whether the rename rewrites `packages/paratext/CHANGELOG.md` | **No.** A published changelog states what was true at that version; editing it makes the record lie to make a grep clean | Taken | 2026-09-20 | — |
| D-089 | The columns this table gains | **`Superseded by` now, `Intent` not yet.** A reversal had nowhere structural to land, so D-005 and D-012 carry reversibility as prose the lock cannot read. `Intent` would be 72 judgement calls about rows already closed — a separate pass | Taken | 2026-09-20 | — |
| D-090 | Which document holds the four numbered rules five sites were citing | **`CLAUDE.md` one level above this repo, not `AI_NATIVE_SDLC.md`.** That document has six stages and no numbered rules; the four rules — hand off through a file, a feedback loop before you start, a human accepts, a check would have caught it — are `CLAUDE.md`'s, and rules 1, 2 and 3 say exactly what the citing text claimed | Taken | 2026-09-20 | — |
| D-091 | Three bellpull cells that fail only under a full parallel run | **Superseded 2026-09-21: to be chased, not recorded.** The owner's instruction is that no failure stands — "every failure we must fix or improve until it reaches our own measurements satisfaction" — so the posture below is the wrong one and is kept only as the record of what was known. **What is known now.** The trigger is not CPU: sixteen busy loops did not reproduce it, and a `matrix.test.ts` run under them passed 23 of 23. It appeared six times while CI watches, `gh` polls and pushes were competing for process slots, and **not once in four consecutive clean `ci:local` runs** afterwards, which points at concurrent *spawns* rather than load — the same shape `exit-hook 17 / 21` and `conformance 121 / 122` showed the same evening. **One theory is refuted rather than open:** the file calibrates its deadlines from a measured cold start, so a loaded runner might have inflated them without bound; measured, a cold `node` goes 32 ms idle to 278 ms under sixteen loops, which puts `budget()` at about 1.1 s and not at minutes. **Named 2026-09-21, which is what every earlier occurrence lacked:** one run finally reported the case rather than the file — `the deadline, end to end (R2, Y10) > ignores SIGTERM: the parent still settles, timedOut is true, and the partial output survives`. The mechanism is the one the file's own header already describes. `beforeAll` measures a cold `node` once and every deadline is a multiple of it, so a calibration taken while the machine is briefly idle sets a budget that a later loaded run outruns: the stubborn child has not installed its `SIGTERM` handler yet, the first rung kills it by the platform's default action, and the assertion that its partial output survives fails. The file asserts that output line *first* because it is proof the child was armed — under load it is proof it was not. The fix that follows is a handshake: start the deadline when the child says it is armed rather than when `run()` is called, which is a change to `run()`'s test design and carries its own proof. Old text: **Recorded, not chased: `src/matrix.test.ts`, `src/which.test.ts` and `src/escape.test.ts` spawn real children against deadlines and starve when the whole monorepo runs at once.** Isolated, the three pass 121 of 121; across four full `ci:local` runs on identical trees two failed and two did not. Neither file names the renamed artifact. A deadline measured against a loaded machine is measuring the machine. **Extended 2026-09-21: the set is larger than bellpull and it reaches a published number.** `conformance` lost 1 of 122 under one full `ci:local` and passed 122 of 122 on its own and on the next full run. Worse, a **compat row** moved: a full oracle run taken while CI watches and a `ci:local` were in flight read `exit-hook 17 / 21 ▼ -4`, and the same binary on the same tree read 21 / 21 alone and 21 / 21 on the next full run. The ratchet would have failed CI on that drop, and it would have been the machine. `cross-spawn`'s note already states the rule for its own row — "a control of 67 / 68 on this case means the machine, not the target, which is a reason to read the TAP before re-recording anything" — and this is that rule generalised: **a suite that spawns processes must not be re-recorded from a loaded run** | Taken | 2026-09-20 | — |
| D-092 | How far the `design.md` rename actually reached, correcting D-078 | **Twelve references in two files, not 130 files across three repos.** `eslint/` tracks no `.sdlc/` directory, no `docs/intents/` and no `intent.md`: its 130 hits were all inside a gitignored `.claude/worktrees/` checkout or an untracked `tmp/` corpus vendored from another tool. `agents/` has none either — its two hits are `08-scorecard-design.md` and a `tech-design.md` table row, different files that happen to end in the same eight characters. The real remainder was `AI_NATIVE_SDLC.md` (10) and the root `CLAUDE.md` (2), both now done. D-078 counted with `find` where it had to count with `git ls-files`, and an untracked file is not a file this repository owns | Taken | 2026-09-20 | — |
| D-093 | Does `burgee`'s public barrel stop re-exporting `renderHelp`, `serveMcp`, `toolsOf` and `schemaOf` | **Owner — and the measurement says it is smaller than it looked.** Interleaved A/B, 21 alternating spawns per arm, medians: dropping `help`, `mcp` and `schema` from the import graph gives **1.33x and 1.44x** on two runs, about a quarter of `import 'burgee'`. But burgee's cold-start overhead above bare node is ~39 ms, so the ~6 ms saved moves `burgee ÷ cac` from 2.03 to roughly 1.94. That is a major version bump, three new subpaths (`burgee/help`, `burgee/mcp`, `burgee/schema`) and a migration note, for 5% of a ratio that has to halve. Default: **not now** — worth doing when something else brings the ratio close, not on its own. Recorded because an earlier report of this put it at -27.7 ms in raw milliseconds and made it look like the single big lever; interleaved, it is not. | Owner — default stands | 2026-09-21 | — |
| D-094 | The Version PR deadlock: fix the token or keep unblocking by hand | **Owner — `RELEASE_BOT_PAT` is the only real fix, and it is a secret only you can set.** A PR opened with `GITHUB_TOKEN` raises no workflow runs, so it sits at 0 checks against 3 required ones. This has now blocked **#299, #367, #383 and #393**, each needing a manual close-and-reopen; one release took four attempts. The workflow's own comment claimed the default token "keeps this workflow working until the secret is added" — it is exactly the token that does not, and that sentence is corrected. Default until the secret exists: **unblock by hand, update-branch before reopening, and wait for the run to settle** — a reopen cancels anything in flight and the Quality Gate then fails in four seconds on `cancelled` sub-jobs rather than on anything real. | Owner — default stands | 2026-09-21 | — |
| D-095 | `conventions/consistent-existence-index-check` fires 48 times after the dependabot bump: suppress it, or follow it | **Pinned to the default it had, and the gap fixed upstream.** `eslint-plugin-conventions` 6.0.0 flipped this rule's default `preferred` from `in` to `Object.hasOwn`; diffed against 5.3.5 that single field is the whole behaviour change, the rule body is otherwise identical. The new default is wrong for TypeScript — `in` is the narrowing operator and `Object.hasOwn` is not, verified with `tsc --noEmit --strict`: reading a union member behind a `hasOwn` guard fails with TS2339, "Property 'reason' does not exist on type", naming the whole union rather than the member that has it, where the `in` guard compiles — and all 48 sites here are discriminated unions. Pinned `{ preferred: 'in' }` in `eslint.config.mjs` rather than turned off, so the consistency check still runs and the five deliberate `Object.hasOwn` uses in seniority keep their own disables. Nothing was silently rewritten: the rule refuses an autofix across the prototype boundary. The rule's page said none of this and now does — `ofri-peretz/eslint#1083`, merged | Taken | 2026-09-21 | — |
| D-096 | linegauge's three B4 ceilings, breached by #389's laziness | **Owner — 111 bundled bytes an entry for about 9 ms of import time.** #389 made the five Unicode property classes lazy and cost a measured **141 bytes** on each of `linegauge`, `linegauge/wrap` and `linegauge/slice`, attributed by rebuilding the three commits around it; all three were under their ceilings before it and over after. Thirty of those were storage rather than laziness — one object keyed by name, whose property names a minifier cannot rename — and #400 recovered them (`linegauge` 6,323 → 6,293, `wrap` 11,278 → 11,264, `slice` 8,954 → 8,924, and `burgee` 58,060 → 58,027 because linegauge is in its graph). **The remaining ~111 are not recoverable by engineering**: writing the two joined source strings out literally to drop the array and its `.join()` calls measures **+73 bytes**, not fewer. What they buy is `width.js` importing in 5.95 ms instead of 15.30. Default: **the trade stands and the three ceilings move**, which `.sdlc/bands/**` says only the integrator may do. `paratext` at 11,122 against 11,100 is a separate, older breach — it measures the same at all three commits | Owner — default stands | 2026-09-21 | D-099 |
| D-097 | What is actually left in the compatibility programme | **Nothing that is a gap. Every remaining case on every active row is a ceiling with a named cause, and the accounting is closed.** Stated because two earlier readings of this were wrong in opposite directions: `FINISH-ALL.md` opened at “385 gradeable” and the summary given on 2026-09-21 said the rest was “gated on owner decisions” — it is not, the decisions were already taken. Case for case: **cosmiconfig 57** = 54 the absent YAML parser (constraint 3, no format parser bundled — the row calls it “a product decision, not a defect”), 1 a declared `controlFailures`, 2 a linux-only XDG pair in `conditionalCases`. **dotenv 61** = 27 `.env.vault`/`DOTENV_KEY`, which dotenv itself deprecated in favour of dotenvx and this package declines, and 34 needing `config()` to default `processEnv` to `process.env`, which R11 forbids and two locks enforce. **rc 1** = the same R11. **lilconfig 10** = the `jest.mock` of a builtin that no hoist reaches, a declared blind spot with no retirement date, measured and reverted. **term-img 6** = D-030, image takes a Buffer so `node:fs` stays out. **clack 3** = `guide.test.ts`, two drawings and one `updateSettings` imported from `@clack/core`, which U6 forbids depending on. **ansi-escapes 3** = CSI, which paratext states out of scope. **terminal-link 2** = reading inside another package's module object. **cli-table3 1** = `cell-test.js` dying at load, which costs the control the same. So the remaining programme is **three unbuilt front-ends** (D-004: meow's harness is ready at a control of 144 / 148, cac and citty deferred because their downloads are bundling rather than adoption) and `closeout`'s signal-exit, whose control does not yet clear its own reference | Taken | 2026-09-21 | — |
| D-098 | Nine changesets that can never be consumed, and a Version PR that keeps opening empty | **Private packages get versions and changelogs; `privatePackages.version` is now `true`.** `.changeset/` is a queue whose contract is that a release empties it, and `compat-oracle` is private, so with versioning off its changesets were never consumed — nine had accumulated. Because `changesets/action` opens a Version PR whenever the directory is non-empty, every push to `main` raised a PR whose `changeset version` produced **no file changes at all**: #399 and #408 were both empty `chore(release): version packages` commits, closed by hand, each sitting at 0 checks against 3 required ones. The shape was self-inflicted — `Changeset present` requires a changeset for any change under `packages/`, which obliged authors to write files the configuration guaranteed could not be consumed. The alternative was to exempt private packages from the check and delete the nine; **versioning them is the better answer because `compat-oracle` is the instrument every published compat number comes from**, and on 2026-09-21 alone its behaviour changed three times in ways that moved those numbers. A changelog for the instrument is worth more than a clean `.changeset/`. Nothing is published: `release.yml` skips `private === true` before it reaches npm, and `tag` stays `false`. `docs`, the only other private package, is in `ignore` and unaffected. `scripts/changeset-reachable-lock.test.ts` holds it, and fails on the old config with the nine named | Taken | 2026-09-21 | — |
| D-099 | Why the B4 breach looked like it needed the owner, when the loop already owns it | **It does not need the owner, and saying it did was the error.** `control-bands.yml` runs weekly, records every series, evaluates Western Electric rules and opens a **Stage 1 intent PR** on a 2σ breach — the policy is already built. What made it look dead: on 2026-09-21 the watcher reported `core-bundled-bytes: 2σ (latest 40562)` while the tree measures **58,027**, and 16 of 19 series had no point from the newest results document. Both facts are innocent. The complete four-axis run is `bench.yml`'s **Tuesday** cron; every other document in `benchmarks/results/` is a partial run from a pull request that writes `{"status":"not-run","reason":"not selected by --axis"}` for the axes it skipped, and the collector reads `<id>.value` off it, finds nothing and moves on. So 40562 is not stale data — it is the newest *complete* measurement, taken before the ~17.5 KB the gate is red about. **The gap was a sentence, not a mechanism:** `latest 40562` reads as *now* and means *last Tuesday*. `unmeasured()` now prints, beside every band, which document failed to measure it and what the band is scoring instead. It is deliberately **not** a gate — a partial run leaves most bands dark and failing on that would fail on every PR, which is how a check earns the reputation that makes the next real red go unread. The B4 breach routes itself at the next complete run | Taken | 2026-09-21 | — |
| D-100 | B4 measured the entry chunk, and the entry chunk is not what a consumer loads | **Corrected against itself, in the direction that costs us.** #415 moved B4 from `--outfile` to `--splitting --outdir` and published the **entry chunk** as `bundled`. That is wrong, and the reason is a detail of how esbuild splits: a module reachable both statically from the entry *and* dynamically from some branch goes into a **shared chunk**, which the entry then loads with a plain `import` statement — at startup, in a separate file the entry-chunk number does not count. Measured on `burgee/commander`: entry chunk **51,257 B**, initial load **59,157 B**, the difference being a 7,681-byte chunk holding `schema`, `plugin`, `definition` and `manifest`. `bundled` is now the transitive closure over `import-statement` edges, `dynamic-import` edges still cost nothing until they run, and every published figure moved against us on the same commit that introduced them: core 19,540 → 44,287, `burgee ÷ cac` 1.869 → 4.237, `burgee ÷ commander` 1.311 → 1.514. `core-under-52kb-bundled` stayed met at the honest number. Found before #415 merged, which is the only reason this is a correction and not a retraction | Taken | 2026-09-21 | — |
| D-101 | D-093 reversed: the root barrel does stop re-exporting the five optional modules | **Reversed, on the number D-093 said it did not have.** D-093 declined the split because it moved `burgee ÷ cac` from 2.03 to "roughly 1.94" — 5% of a ratio that has to halve — and called it not worth a major. Measured with the corrected instrument (D-100) it is not 5%: the barrel was keeping `help.ts`, `mcp.ts`, `schema.ts`, `plugin.ts`, `manifest.ts` and `seniority/precedence` live for every consumer, and dropping the value half takes the initial load **44,663 → 28,637 bundled bytes** (−36%), the on-disk static graph **60,823 → 42,223**, the modules Node loads for `import 'burgee'` from **22 to 17**, and cold start **2.113 → 1.683**. Against the pre-#415 published figures that is 57,880 → 28,637 and 2.567 → 1.683. The earlier estimate was small because it measured dropping three modules against an instrument that was inlining the other two anyway. Breaking, narrowly: every moved value has a subpath (`burgee/help`, `burgee/mcp`, `burgee/schema`, `burgee/plugin`, `burgee/config`) and **every `type` stayed**, so a typed program that called none of the moved functions needs no change | Taken | 2026-09-21 | — |
| D-102 | `lighter-than-cac`, `lighter-than-commander` and `cold-start-at-or-below-cac`: what is left, and whether ≤1 is reachable | **Owner — three claims have a measured floor above their own gate, and closing the gap means deleting the product, not optimising it.** Everything engineering can reach has now been taken (D-100, D-101) and the residual is structural. **`lighter-than-cac` (2.740):** cac is 10,457 bundled bytes of parser and help renderer. burgee's 28,637 is parser + `validate.ts` (coercion, choices, relations, Standard Schema) + `seniority` precedence + `closeout`'s registry and terminal restore + agent detection + package discovery. Stripping every remaining conditional branch — `seniority`'s explain half, `plugin.ts`'s `validate`, closeout's hang deadline and its report — measures a floor near **23,600 bytes, or 2.26×**. **`lighter-than-commander` (1.514):** our `commander/command.ts` is 33,487 bundled against commander's 27,226, and the front-end also carries `bellpull/cross-spawn` (5,060 B, and it cannot go lazy — `commander/sync.test.ts` holds `parse()` synchronous and ~23 `executableSubcommand` cases in commander's own suite mock the spawn inside it, so the compat claim of 1360 / 1360 pays for the bytes). Even at byte parity with commander's own command.js the ratio lands near **1.09**: the claim asks our commander to be smaller than commander while also carrying a manifest, a schema and an MCP server. **`cold-start-at-or-below-cac` (1.683):** `import 'cac'` is one file in 4.2 ms; `import 'burgee'` is 17 in 17.4 ms, and the remaining five that could go lazy would leave ~12 — a ratio near **1.5**, not 1. Three options, and the default is the third: **(a)** delete features until the numbers fit, **(b)** restate the two cac rows against what a consumer actually has to install to reach parity — cac plus a config resolver plus a shutdown binder — which is the comparison U5's own words describe and the shape `roundel/tokens` already uses for picocolors (its entry says picocolors is "the *weight bar* the bet names, not a package roundel replaces"; cac was entered in `PAIRS` as a drop-in incumbent and there is no `burgee/cac`, no cac compat row), **(c)** leave all three on the README as **not met** with the measured floor beside them. Default while unanswered: **(c)** — a claim we cannot meet stays on the page saying so | Owner — default stands | 2026-09-21 | — |
| D-103 | `agent-tokens-40pct` has never been measured, and measuring it costs money or plan usage | **Owner — it needs a credential only you can issue, and here is the bill.** B1 spawns `claude -p` for 5 tasks × 2 builds × 5 runs = **50 agent runs** at 12–15 turns each, and it refuses to run without `CLAUDE_CODE_OAUTH_TOKEN` or `ANTHROPIC_API_KEY`. On `ANTHROPIC_API_KEY` the estimate is **$5–15 for a complete pass** (Sonnet, ~60 K input and ~3 K output per run, cache-dependent); on `CLAUDE_CODE_OAUTH_TOKEN` there is no marginal charge and it draws on the subscription instead. Separately, `DEFAULT_MODEL` is pinned to `claude-sonnet-4-5`, which is two generations behind — the pin is deliberate (a model change starts a new band history) but it should be re-pinned before the first real run rather than after, since the first run is what the band history starts from. Default while unanswered: **the axis keeps skipping loudly and the README keeps saying `unmeasured`**, which `emit.test.ts` enforces — nothing in the suite can turn a run that did not happen into a number | Owner — default stands | 2026-09-21 | — |
| D-104 | How far the plugin surfaces are actually built | **The data half is built everywhere and the feedback loop exists once.** Measured 2026-09-21. PRINCIPLES 7 asks three things of an extension surface: the plugin is data validated against a published schema, there is a **`check` command that renders it every way it can be seen**, and the bar is **measured weekly** — an agent given the schema and one example writes a passing plugin in one turn. Eight of nine packages publish `./plugin` and `./schema.json` and all eight export `validate`, so the first is done. The second exists in **one** package (`flagstaff`) and the third in **one** case (`evals/cases/flagstaff-plugin-from-schema.json`). `linegauge` has no extension surface at all. A surface nobody can check is a surface nobody outside this repository can write against, which is the difference between an extension point and a published interface. `scripts/extension-surface-lock.test.ts` holds the table so the gap can close but cannot drift, and enforces the one implication that matters today: a package that ships `check` ships an eval, because a loop without a measurement is the bar claimed rather than met. **Not** made a failing gate — the six missing commands are scheduled, and failing on scheduled work is how a check teaches people to read red as normal. `check` cannot be shared: flagstaff's is 200 lines of rendering a spinner in five output modes, and every package's renders its own contribution kinds | Taken | 2026-09-21 | — |
| D-105 | The largest single item in two packages' bundles is a JSON file, and neither is reachable lazily | **Measured, named, and not changed tonight — the sync API is what blocks each one.** Scanning every entry point's startup graph after D-101 turned up two places where a *data file* is the majority of the bundle. **`paratext`: `schema.json` is 6,413 of 11,113 bundled bytes (58%)** — the family plugin schema, imported by `capability.ts` because `register()` validates every capability against it, and `builtins.ts` registers seven through the public `register` at module load. That is deliberate (flagstaff U4: "a built-in cannot grow a power a stranger's plugin lacks") and it means a program printing one hyperlink re-validates our own capabilities on every start. **`flagstaff/ora`: `spinners.json` is 31,188 of 55,467 (56%)** — `cli-spinners`' whole corpus, reached from `set spinner()` for the named case and exported as `spinners` because that is ora's own API. Neither can go lazy: `register()` is a synchronous public call and `set spinner()` is a **setter**, so an `await import()` is not available at either site, and both are the same class as `bellpull/cross-spawn` in the commander façade (D-102) — a contract bought with bytes. Reading the JSON from disk instead of inlining it would move the bytes rather than remove them and would break a bundled consumer outright. Recorded because the numbers are worth knowing and because the shape is now the repository's most common source of weight: **`paratext ÷ ansi-escapes` at 2.556 is 58% plugin schema**, and `.sdlc/PLAN.md` D1 already decided the foundation tier is compared against the incumbent's *tree*, where `paratext` is not the outlier this row makes it look. ora carries the identical corpus, which is why `flagstaff/ora ÷ ora` is 0.932 rather than 0.41 | Taken | 2026-09-22 | — |
| D-106 | caique reaches `closeout/exit-hook` — a compat façade — from inside the family; should it use `onExit` instead | **No, and the test said so in 180 ms.** The argument for the swap was good on paper: `closeout/exit-hook` is that package's *drop-in path for the `exit-hook` package* (its design R6), published for somebody migrating off that dependency, and reaching for it from inside the family means caique ships the incumbent's semantics and pays **1,850 bundled bytes** for a compatibility layer it is not the consumer of. `closeout/cursor`'s `hideCursor(stream, onExit)` even takes the registrar as a parameter, so the swap was two lines and it typechecked. It also broke `raw.test.ts`: *"SIGINT puts the cursor back, and still ends the process"* expected **130** and got `null`, and the SIGTERM case expected **143** and got `null`. The façade **re-raises the signal after the handlers run**, so the process leaves with 128 + n the way a signalled process must; `onExit` on its own restores the cursor and then does not. That is the 1,850 bytes, and they are not overhead — they are E5, and caique's own suite is what proves it. Reverted. Recorded so the next reading of this file does not re-derive the same two-line change: the rule is that a façade may be the right internal dependency when what it wraps is a *behaviour* and not just an API, and the way to find out is to make the change and run the suite | Taken | 2026-09-22 | — |
| D-107 | D-091's "load-sensitive suites" — are the tests flaky, or is the gate | **The gate. Measured, and the fix costs nothing.** For weeks a rotating cast of suites failed inside `turbo run test` and passed alone — `bellpull`'s matrix and escape, `caique`'s conformance, the `exit-hook` compat row, `seniority`'s lilconfig and rc, `burgee`'s dev loop — and it was recorded as a property of those suites. It is not. **Every failure was `Error: Test timed out in 5000ms.` and every failing suite touches the filesystem or spawns a process**, and the battery's own concurrency is the variable: forced so nothing came from cache, `--concurrency=1` was 2 of 2 clean, `--concurrency=2` 2 of 2 clean at 82 s, `--concurrency=3` 1 of 2 failed, and the default of 10 failed **2 of 4** at 45 s. The clincher is a timing: `seniority`'s `rc()`, the call inside three of the failing cases, runs in **0.5 ms idle and 159 ms under load** — two orders of magnitude short of the 5,420 ms the cases were killed at. The test was never the problem and neither was `rc()`; vitest's 5 s default is a budget for a test body that *computes*, and these bodies wait on a disk while ten sibling tasks use it. Two fixes were measured and one was kept. **Bounding turbo to two tasks works and costs +37 s on every push, forever.** **Capping vitest's worker pool to 2 does not work** — 2 of 4 either way, because the contention is between tasks and not inside one; it was written, measured, and reverted rather than shipped with a confident comment. What ships is `vitest-timeouts.config.ts`: 30 s for a test and 60 s for a hook, shared by every package suite, chosen to sit far above "the machine is busy" and far below "forever" so a genuine hang still fails a build. Result: **8 of 8 clean**, four forced batteries at full concurrency and four `ci:local` runs, against a baseline of 2 in 4 failing | Taken | 2026-09-22 | — |
| D-108 | One byte-identical plugin schema across nine hosts: every host now pays for every host's schema definitions | **The design stands, and the bill is written down rather than absorbed.** `scripts/plugin-schema-lock.test.ts` holds one `schema.json` byte-identical across every package with a `src/plugin.ts`, authored in flagstaff and copied by `schema-sync.mjs`. That is what makes *one contract* true: a plugin author validates against one file, and the same object registers into any subset of the family that is installed. Making `linegauge` the ninth host added three schema definitions — `widthRange`, `widthOverride`, `widths` — and the schema went **9,714 → 11,629 bytes in source, about 1.3 KB minified into `dist/`**. Every host that *imports* it to validate against pays that, for a definition only linegauge reads: five flagstaff budgets moved, two paratext budgets moved, and six of the seven foundation layers' recorded pack weights moved with them. Nothing here is flagstaff's or paratext's code getting bigger. **Not changed, and the alternatives are why.** Per-host schemas would end byte-identity, which is the property the lock exists for and the reason a plugin written for one host is legible to the next. A schema reference to a hosted URL would put a network fetch inside `check`. **Fragments were the answer, and the first CI run is what forced the question.** paratext's default import went 11,122 → **12,465** bytes, a ratio of 2.865 against `ansi-escapes`, for a definition it never reads — it only ever read the capability definition, 2,121 of 8,141 minified bytes. Raising its budgets to absorb that was tried first and was the wrong answer. `scripts/schema-sync.mjs` now also writes per-host *fragments* — exactly the definition a host validates against — and `plugin-schema-lock.test.ts` asserts each deep-equals its source definition, so there are still not two copies free to disagree. paratext imports the fragment and ships the full contract as data it does not import: **6,736 bytes, ratio 1.548** — lighter than it was before linegauge's definitions existed, and the largest single fall any foundation row has had. The published `schema.json` is still one byte-identical file. Rough rule from the two data points: **a new host costs the other eight about 150 bytes per definition each** | Taken | 2026-09-22 | — |
| D-109 | D1 puts 171 bytes on `burgee/commander`, a front end that never deprecates anything, over a ceiling set that morning | **Raised to 59,450, the next fifty above the 59,421 measured, and the reason is the plugin host.** D1 refuses `deprecated: true` in `definition.ts`, the door `defineCommand` and `Manifest.use()` share. The commander front end never declares a deprecation through it — it accepts commander's bare form and bypasses the door for its own commands — but `plugin.js` imports `definition.js`, so every graph that can register a plugin carries the check, and a plugin registered through a commander program must be read by exactly the code a first-party command is (N6). **Not changed, and the alternatives are why.** Keeping the check out of the plugin door would let a plugin ship a deprecation with no replacement that a first-party command cannot — the asymmetry `checkCommand` was merged into one function to end. A shorter message would save about 40 of the 171 bytes and tell the author less about the fix, which is the check's only job. `lighter-than-commander` was already unmet at 1.515 before this, and 171 bytes moves it by 0.004 | Taken | 2026-09-22 | — |
| D-110 | May a published package depend on anything outside this repository — as a dependency, a peer or an optional dependency? (Z3, K1; GAPS B17) | **No. Every production dependency is in-family.** The owner, 2026-09-23: *"All our packages dependencies should be 'in-family'"*, *"we should avoid any external dependencies"*. A published package may depend on its siblings in this repository and on nothing else, and that covers all three fields npm installs from — `dependencies`, `peerDependencies`, `optionalDependencies`. Z3's and K1's *"zero runtime dependencies"* is restated to this, because it is what the family has always been: burgee, caique and flagstaff depend on the layers below them by design. `scripts/package-shape-lock.test.ts` held `dependencies` only; it now holds all three, proved by giving roundel an external `chalk` peer, which turned exactly that case red. devDependencies — test tooling, benchmark incumbents — are not shipped and are outside the rule; so is `compat-oracle`, which is private and whose peers are the incumbents it grades | Accepted | 2026-09-23 | — |
| D-112 | Must every command carry a group? (burgee M1, GAPS B1) | **No.** A command *may* carry a group, and help renders commands under it. Forcing one on a three-command program is the ceremony Z1 forbids. M1 restated; it is met by `commandSections`. Taken under the owner's delegation, 2026-09-23 (*"You should be able to close the open decisions yourself"*; *"we've discussed it"*) — the recommendation recorded in `.sdlc/GAPS.md` | Taken | 2026-09-23 | — |
| D-113 | Does `-` mean stdin for a file positional? (burgee S4, B2) | **Yes.** `ArgumentSpec` gains `type: 'file'`, and a `-` there hands the handler the injected stdin. To build. Taken under the owner's delegation, 2026-09-23 (*"You should be able to close the open decisions yourself"*; *"we've discussed it"*) — the recommendation recorded in `.sdlc/GAPS.md` | Taken | 2026-09-23 | — |
| D-114 | Does `--json` take fields? (burgee N14, B3) | **Yes, as `--json=<a,b>` only**, so `cmd --json positional` is never misread; `--json=` alone lists the valid fields and an unknown field refuses naming them. To build. Taken under the owner's delegation, 2026-09-23 (*"You should be able to close the open decisions yourself"*; *"we've discussed it"*) — the recommendation recorded in `.sdlc/GAPS.md` | Taken | 2026-09-23 | — |
| D-115 | A compact non-JSON `agent` output format? (burgee N15, B4) | **Deferred past 1.0.** Its justification is token cost, and B1 — the agent-token measurement — has never run. N15 stays Not built and is not a 1.0 gate. Taken under the owner's delegation, 2026-09-23 (*"You should be able to close the open decisions yourself"*; *"we've discussed it"*) — the recommendation recorded in `.sdlc/GAPS.md` | Taken | 2026-09-23 | — |
| D-116 | Can `--schema` drill below a command path? (burgee N13, B5) | **Yes: `--schema <path> --field <name>`** returns one field of one command's schema. To build. Taken under the owner's delegation, 2026-09-23 (*"You should be able to close the open decisions yourself"*; *"we've discussed it"*) — the recommendation recorded in `.sdlc/GAPS.md` | Taken | 2026-09-23 | — |
| D-117 | A generated `config explain`? (burgee V8, B6) | **Yes**, synthesised for a program that reads config, generated from the resolver's own precedence, never hand-written. To build. Taken under the owner's delegation, 2026-09-23 (*"You should be able to close the open decisions yourself"*; *"we've discussed it"*) — the recommendation recorded in `.sdlc/GAPS.md` | Taken | 2026-09-23 | — |
| D-118 | Author-declared error classes? (burgee E7, B7) | **Yes: `defineError({ name, code })`** adds a row to `CLASSIFIED`, and two classes claiming one exit code fail at startup. To build. Taken under the owner's delegation, 2026-09-23 (*"You should be able to close the open decisions yourself"*; *"we've discussed it"*) — the recommendation recorded in `.sdlc/GAPS.md` | Taken | 2026-09-23 | — |
| D-119 | A marker for runtime-completed options? (burgee D3, B8) | **Yes, opt-in per option (`dynamic: true`)**; every other option's completion stays static. To build. Taken under the owner's delegation, 2026-09-23 (*"You should be able to close the open decisions yourself"*; *"we've discussed it"*) — the recommendation recorded in `.sdlc/GAPS.md` | Taken | 2026-09-23 | — |
| D-120 | How does a non-TTY prompt exit 2 and a cancelled one exit 4? (burgee P2, P3, B9) | **Without a new dependency edge:** burgee maps a thrown error carrying `code: 'USAGE'` to exit 2 and `code: 'CANCELLED'` to exit 4 — the codes caique's refusals already carry. burgee does not import caique. To build. Taken under the owner's delegation, 2026-09-23 (*"You should be able to close the open decisions yourself"*; *"we've discussed it"*) — the recommendation recorded in `.sdlc/GAPS.md` | Taken | 2026-09-23 | — |
| D-121 | One call that turns on the behavioural floor for façade programs? (burgee J3, J4, B10) | **Yes: `.burgee({ floor: true })`** on both front-ends, and `--schema` reports which reserved flags a program shadows. To build. Taken under the owner's delegation, 2026-09-23 (*"You should be able to close the open decisions yourself"*; *"we've discussed it"*) — the recommendation recorded in `.sdlc/GAPS.md` | Taken | 2026-09-23 | — |
| D-122 | Plugin hooks beyond preRun, postRun and onError? (plugin surface, B11) | **Add `parse` (argv in, argv out) and `shutdown`.** Help and config hooks wait for an adopter who asks. To build. Taken under the owner's delegation, 2026-09-23 (*"You should be able to close the open decisions yourself"*; *"we've discussed it"*) — the recommendation recorded in `.sdlc/GAPS.md` | Taken | 2026-09-23 | — |
| D-123 | What validates `--schema` output? (schema-is-validated, burgee F1, B12) | **No runtime validator.** burgee publishes `burgee/program-schema.json`, generated from `ProgramSchema`; a test validates real `--schema` output against it with flagstaff's `conforms` walker as a devDependency — one walker in the family, and nothing added to what a user installs (D-110). To build. Taken under the owner's delegation, 2026-09-23 (*"You should be able to close the open decisions yourself"*; *"we've discussed it"*) — the recommendation recorded in `.sdlc/GAPS.md` | Taken | 2026-09-23 | — |
| D-124 | Where does the lint half of F3, O3 and P1 live? (B13) | **In the Interlace ESLint monorepo, not here** — that repository is where this family's lint plugins are built, published and benchmarked (one repo per family). The three rows keep their lint half open and point there. Taken under the owner's delegation, 2026-09-23 (*"You should be able to close the open decisions yourself"*; *"we've discussed it"*) — the recommendation recorded in `.sdlc/GAPS.md` | Taken | 2026-09-23 | — |
| D-125 | Are flagstaff's built-in components plugin contributions? (flagstaff R4, B14) | **No — restated.** Glyphs, spinners and borders are registered through the public door; `spinner`, `progress`, `tasks`, `box` and `table` are factories with options the component shape cannot express, and stay so. Taken under the owner's delegation, 2026-09-23 (*"You should be able to close the open decisions yourself"*; *"we've discussed it"*) — the recommendation recorded in `.sdlc/GAPS.md` | Taken | 2026-09-23 | — |
| D-126 | `fromBase16` / `fromITerm` theme import? (roundel R11, B15) | **Deferred past 1.0.** Its own evidence row reads *"hypothesis — measure before lock"*; nothing asks for it. R11 is not a 1.0 gate. Taken under the owner's delegation, 2026-09-23 (*"You should be able to close the open decisions yourself"*; *"we've discussed it"*) — the recommendation recorded in `.sdlc/GAPS.md` | Taken | 2026-09-23 | — |
| D-127 | Can façade commands say what they do to the world? (agent-surface-declared R6, B16) | **Yes: `.effects()` on commander commands**, as yargs' front-end already has, so a façade command stops being withheld from MCP by default. To build. Taken under the owner's delegation, 2026-09-23 (*"You should be able to close the open decisions yourself"*; *"we've discussed it"*) — the recommendation recorded in `.sdlc/GAPS.md` | Taken | 2026-09-23 | — |
| D-128 | Keep the three unmet weight and speed claims? (claims, u5-weight-claim, B18) | **Keep them, published as not met.** Restating a claim to make it pass is the thing the claims table exists to prevent; the gap is measured and shown. Taken under the owner's delegation, 2026-09-23 (*"You should be able to close the open decisions yourself"*; *"we've discussed it"*) — the recommendation recorded in `.sdlc/GAPS.md` | Taken | 2026-09-23 | — |
| D-129 | Accept the designs still in draft or review? (B19) | **Accepted:** linegauge, closeout, bellpull, seniority, caique, paratext, compat-oracle and schema-is-validated. Acceptance is of the design; each spec's `Not built` rows stay open and are built against it. Taken under the owner's delegation, 2026-09-23 (*"You should be able to close the open decisions yourself"*; *"we've discussed it"*) — the recommendation recorded in `.sdlc/GAPS.md` | Taken | 2026-09-23 | — |
| D-130 | Accept the requirement restatements already written? (B20) | **Accepted:** compat-oracle C4, R1, R3, C5; burgee U1, U10; flagstaff R10's dependency sentence. Each replaces a sentence the tree measurably outgrew, and the original stays verbatim beside it. Taken under the owner's delegation, 2026-09-23 (*"You should be able to close the open decisions yourself"*; *"we've discussed it"*) — the recommendation recorded in `.sdlc/GAPS.md` | Taken | 2026-09-23 | — |

## How a question gets closed

1. It is raised in the intent that owns it, under `## Open questions`.
2. It is answered here — a row, an answer, a date, and whether it was **taken** by an agent
   or **accepted** by the owner.
3. It is struck from the intent's `## Open questions`, because it is no longer one.

## How a decision gets reversed

A closed row is never edited. Reversal is a **new row** with the new answer, and the old
row's `Superseded by` pointing at it — so the ledger reads as a history rather than a
current state, and `git log` is not the only place the change survives. An em dash means
nothing reverses it. The lock checks the reference resolves; it cannot check the reasoning,
which is the author's.

`npm run decisions` lists every question still open across every intent, so the count can
only go down deliberately. A question that has been open for two weeks is a decision nobody
is making, which is itself a decision — made slowly, and by default.
